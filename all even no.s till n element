#include <stdio.h>

int main() {
    int n;
    int even=2;
    scanf("%d", &n);
    while (n>=2)
    {
         printf("%d ", even);
         n--;
         n--;
         even++;
         even++;
    }
    
    return 0;
}

//OR

#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int i = 2;

    while (i <= n)
        {
        printf("%d ", i);
        i += 2;
        }

    return 0;
}